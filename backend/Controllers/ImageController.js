exports.uplaodImage = async (req, res) => {
  try {
    //return the url
    //store image in image folder
    //then upload to cloudnary
    //after getting ul delete image from folder
    //send url to frontend
  } catch (error) {
   //send enputy stirng''
  }
}